# Public origin used for canonical URLs, Open Graph URLs, robots.txt and sitemap.xml.
# Set this to your Vercel production URL or custom domain, without a trailing slash.
NEXT_PUBLIC_SITE_URL=https://flowpilot-demo.vercel.app
